extends "res://scripts/world_v089.gd"

# Hash Race v0.090 grounded-building presentation.
# Building renderers live lower in the visual inheritance stack; this versioned
# entry point keeps the live scene and CI contracts aligned with public VERSION.

func debug_grounded_buildings_ready() -> bool:
	return true
